# Create Tourist Place Service 🏝️

Microservicio para registrar nuevos lugares turísticos.

## 🚀 Instalación y Ejecución
```sh
pip install -r requirements.txt
uvicorn src.main:app --host 0.0.0.0 --port 5001 --reload

📄 API Endpoint
```sh
POST /api/place